# 通信面试知识

## UART

Q:
为什么UART需要设计协议？

A:
UART只是字节流，没有消息边界。

因此需要：
- 帧头
- 长度
- CRC
- 状态机


项目回答：

STM32通过UART发送传感器数据，因此设计自定义协议保证数据完整性。


---

Q:
为什么不用TCP保证可靠？

A:
MCU资源有限，UART/RS485场景通常不使用TCP。

可靠性由应用层协议实现。
