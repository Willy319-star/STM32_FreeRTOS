# FreeRTOS 面试知识

## Queue

Q:
Queue有什么作用？

A:
Queue用于任务之间传递数据。

项目中：
SensorTask采集数据，通过Queue发送给处理任务。


深挖：

Q:
Queue传递数据还是地址？

A:
默认复制数据。如果传递指针，需要保证生命周期。


---

## Mutex

Q:
为什么需要Mutex？

A:
保护共享资源。

例如多个任务访问UART发送缓冲区。


深挖：

Q:
什么是优先级反转？

A:
低优先级任务持有锁，高优先级任务等待，而中优先级任务持续运行导致高优先级任务阻塞。
